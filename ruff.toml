line-length = 120
target-version = "py312"

[lint]
select = ["E", "F", "W"]
ignore = [
    "E501",  # line too long
    "E722",  # bare except (existing pattern in app.py)
    "F401",  # unused imports
]

[lint.per-file-ignores]
"tests/*" = ["F401", "E402"]
