line-length = 120
target-version = "py312"

[lint]
# Only catch real bugs, not style nits. Tighten over time.
select = [
    "E9",   # syntax errors
    "F63",  # invalid format strings
    "F7",   # syntax-related
    "F82",  # undefined names
]

[lint.per-file-ignores]
"tests/*" = ["E402"]
